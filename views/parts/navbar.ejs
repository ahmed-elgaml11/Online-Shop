<nav class="navbar navbar-expand-md bg-dark border-bottom border-body" data-bs-theme="dark">
  <div class="container d-flex align-items-center"> <!-- Added d-flex align-items-center -->
    
    <a class="navbar-brand me-3" href="/">Camel</a> <!-- Added me-3 for spacing -->

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" 
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% pages.forEach((page) => { %>
          <%  if(page.slug == 'home'){ %>
            <li class="nav-item">
              <a class="nav-link " href="/">
                <%= page.title %>
              </a>
            </li>  

            <% } else{ %>
              <li class="nav-item">
                <a class="nav-link " href="/page/<%= page.slug %>">
                  <%= page.title %>
                </a>
              </li>
    
              <% } %>
        <% }) %>
      </ul>

      <form class="d-flex me-3" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>

      <div class="d-flex">
        <a class="nav-link text-white me-3" href="/cart/checkout">MY Cart (
          <% if(typeof cart != undefined){ %>
            <%= cart.length %>
          <% } else{ %>
            0
          <% } %>

          )</a>
        <a class="nav-link text-white me-3" href="#">Register</a>
        <a class="nav-link text-white" href="#">Log In</a>
      </div>

    </div>
  </div>
</nav>
